# Goal-Mode Guideline: 24-Bit QuanTrick SNNv2

## Goal-mode contract

**Goal:** deliver a trained, rollout-compatible 24-bit (3x8-bit) QuanTrick SNNv2 and collect validated videos for every configured robot.

**Terminal success condition:** mark the goal complete only after every item in the final completion checklist is checked and its evidence is present on disk. Implementation, unit tests, or a successful training process alone are not completion.

**Non-negotiable gates:** preserve current artifacts; keep legacy 8-bit behavior compatible; do not record until the selected 24-bit model meets the validation-MSE gate; do not count pre-existing videos; do not silently relax a gate.

**Blocked-state rule:** if all specified fallback training runs finish and still fail the MSE gate, retain logs/artifacts, report the measured candidates and failure cause, and request a decision. Do not replace the candidate with the legacy model merely to finish recording.

## Goal condition

Implement PRECiSE-style 24-bit weight quantization as three 8-bit chunks in `/app/lava-dl`, integrate it with SNNv2 training and PyTorch rollout, train a quantized SNNv2 without regressing from the current best full-validation MSE, and record all 16 configured robots.

The goal is complete only when:

- Lava-DL and project tests pass.
- The winning checkpoint uses 24-bit, 3x8-bit decomposed weights during both training and rollout.
- Full-validation MSE is `<= 0.00592308546602726`.
- All predictions and weights are finite and no 24-bit saturation occurs.
- Sixteen new, readable MP4 files—one per `record_robots` entry—exist directly under `/app/one_policy_to_run_them_all/experiments/videos`.
- A manifest identifies the checkpoint, configuration, metrics, robot-to-video mapping, and video validation results.
- Existing checkpoints and videos are preserved and are not counted as new evidence.

True NetX/Loihi multi-synapse deployment is out of scope. Legacy 8-bit HDF5 export must remain unchanged; decomposed mode must reject HDF5 export clearly rather than produce an invalid single-weight representation.

## Ordered TODO checklist

Work through this list in order. Tick an item only after its stated evidence is available.

- [x] **0. Establish baseline and workspace safety.** Record `git status` for both repositories, preserve unrelated dirty files, snapshot the existing video filenames, and copy the long-best metric/config into the new run manifest.
- [x] **1. Implement Lava-DL decomposition primitives.** Add the public decomposition API, quantizer, validation, diagnostics, and legacy-compatible behavior described below.
- [x] **2. Verify Lava-DL behavior.** Pass all focused decomposition/CUBA tests, then the full Lava-DL suite; save command output and versions in the run manifest.
- [x] **3. Integrate SNNv2.** Wire the feature flag, diagnostics, checkpoints, HDF5 guard, rollout loader, PPO settings, recording preset, and all-robot wrapper.
- [x] **4. Verify project integration.** Pass focused compatibility/rollout tests and the full project test suite; save results with the implementation artifacts.
- [x] **5. Run training smoke test.** Complete the two-epoch smoke run with finite loss, gradients, outputs, and zero saturation.
- [x] **6. Run matched candidate trials.** Complete all three 40-epoch candidates, rank decomposed candidates, and record the selection rationale.
- [x] **7. Train and accept a final candidate.** Finish the selected 300-epoch run, or the defined fallback sequence, and satisfy the full-validation MSE gate.
- [x] **8. Prove rollout parity.** Load the final checkpoint through the rollout path and satisfy the fixed-batch parity and finite-output checks.
- [x] **9. Record every robot.** Produce exactly one new video for each of the 16 configured `record_robots` entries.
- [x] **10. Validate and hand off.** Validate videos, create the final manifest/contact sheets, review completion evidence, and only then mark the goal complete.

## Implementation changes

### Lava-DL quantization API

Add a public utility under `lava.lib.dl.slayer.utils` with:

- `SignMode`: `MIXED`, `EXCITATORY`, and `INHIBITORY`.
- `WeightChunk`: chunk tensor, exponent, sign mode, and MSB metadata.
- `WeightDecomposer(target_bits=24, chunk_bits=8)`.
- `DecomposedWeightQuantizer`, a callable compatible with a synapse `pre_hook_fx`.

Required behavior:

- Validate that bit widths are positive and `target_bits` is divisible by `chunk_bits`.
- Support NumPy arrays for diagnostic use and PyTorch tensors for training.
- Use `torch.int64` internally so unsigned chunks remain portable across CPU and CUDA.
- With CUBA scale `64`, calculate `m = clamp(round(weight * 64))`, decompose `m`, reconstruct it, and return `m / 64`.
- For 24/8 mixed mode, reconstruct `m = c0 * 2^0 + c1 * 2^8 + c2 * 2^16`.
- In mixed mode, interpret lower chunks as unsigned and the most-significant chunk as signed.
- In excitatory mode, use unsigned chunks and reject or clamp negative inputs according to the documented saturation policy.
- In inhibitory mode, decompose magnitudes and negate each chunk's contribution.
- Use a straight-through estimator so the discrete reconstructed value is used in the forward pass while gradients pass to the original floating-point parameter.
- Preserve tensor shape, device, and floating dtype.
- Expose reconstruction error and saturation counts for diagnostics.
- Keep existing `quantize_8bit` behavior as the default and backward-compatible path.

Golden vectors must include `8388607 -> [255, 255, 127]`, `-8388608 -> [0, 0, -128]`, mixed-mode `-1`, zero, boundary overflows, and inhibitory values.

### SNNv2 training and checkpoint integration

Extend `snn/train_v2.py` and its configuration with:

```yaml
model:
  weight_quantization:
    mode: legacy_8bit
    target_bits: 24
    chunk_bits: 8
    sign_mode: mixed
    scope: all
```

Rules:

- Existing configs default to `legacy_8bit`.
- The new QuanTrick config explicitly selects `mode: decomposed`, `target_bits: 24`, and `chunk_bits: 8`.
- `scope` supports `all` and `first`; it applies only to Dense/Affine synaptic weights, not the scalar input transform.
- Install the decomposed quantizer as the selected layers' `pre_hook_fx` in ANN, sampler, and SNN modes.
- Reject unsupported modes, signs, scopes, or widths before training.
- Reject `runtime.export_hdf5: true` in decomposed mode with an actionable message.
- Log per-layer raw range, quantized range, reconstruction error, and saturation count at initialization, each full-validation epoch, and final checkpoint selection.
- Save self-describing checkpoint payloads containing `state_dict`, architecture, neuron settings, input encoding, and quantization metadata.
- Continue loading old raw state dictionaries and existing metadata checkpoints.
- Update final best-checkpoint reload logic to extract `state_dict` from either format.

Create `snn/train_v2_config_quantrick.yaml` from the long-best defaults:

- `time_steps: 20`
- `input_weight: 1.0`
- threshold `0.2`
- current decay `0.3`
- voltage decay `0.02`
- 300 epochs, batch size 1024, learning rate `2e-3`
- full validation every 10 epochs
- scheduler factor `0.7`, patience `10`, threshold `5e-5`, minimum LR `2e-5`
- decomposed 24/8 mixed quantization
- HDF5 export disabled
- unique output directories under `snn/experiments/snn_v2_quantrick/<UTC_RUN_ID>`.

### Rollout and recording integration

Propagate quantization metadata through `BootstrapTrainingConfig`, `BootstrapStudentPolicy`, checkpoint loading, PPO defaults, and experiment overrides. Metadata stored in a checkpoint takes precedence; explicit preset values provide compatibility for raw checkpoints.

Add:

- A `record_snnv2_quantrick` preset pointing to the winning checkpoint with matching `time_steps`, neuron dynamics, input encoding, and quantization settings.
- A `record_snnv2_quantrick_all_robots.sh` wrapper following the existing all-robot recording loop.
- Preset and wrapper tests proving the new checkpoint and 24/8 settings reach rollout construction.

Before recording, compare trainer and rollout-loader predictions on the same fixed batch. Require identical outputs within `atol=1e-6`, finite values, and confirmation that every selected synapse has the decomposed pre-hook.

## Verification and training procedure

### Feature tests

In `/app/lava-dl`, add focused tests for:

- All sign modes and golden vectors.
- Exact integer reconstruction for randomized 24-bit values.
- Quantization error bounded by `1 / (2 * 64)`.
- Saturation and invalid configuration handling.
- NumPy and PyTorch parity.
- CPU/CUDA shape, dtype, and device preservation.
- Straight-through gradients.
- Dense/Affine forward parity with a reference convolution using reconstructed weights.
- Unchanged legacy 8-bit outputs and HDF5 export.
- Explicit decomposed-HDF5 rejection.

Run the focused Lava-DL suite, relevant CUBA block tests, then the full Lava-DL test suite.

In `one_policy_to_run_them_all`, test:

- Config loading and CLI overrides.
- Old raw and new metadata checkpoint compatibility.
- Trainer/rollout output parity.
- A one-epoch CPU optimization smoke test.
- Invalid quantization and HDF5 combinations.
- Experiment preset and shell-wrapper wiring.
- The full project test suite.

### Training gates

Use deterministic split seeds `0` and `1` throughout.

1. Run a two-epoch/4096-sample smoke job. Require decreasing or finite loss, nonzero gradients, finite outputs, and zero saturation.
2. Run matched 40-epoch, 50k-train/10k-validation trials:
   - Legacy 8-bit control.
   - Decomposed 24/8 on the first weighted layer.
   - Decomposed 24/8 on all weighted layers.
3. Rank decomposed candidates by full-validation MSE. Break ties using median percentage error, then mean percentage error.
4. Train the winning decomposed scope for 300 epochs on the complete dataset and validation split.
5. Accept only a checkpoint with full-validation MSE `<= 0.00592308546602726`. Report mean and median percentage error against the existing `13.90697%` and `10.76049%` baselines, but do not make them hard gates.
6. If the full run regresses:
   - Fine-tune the existing long-best checkpoint under the winning decomposed mode for up to 100 epochs at `2e-4`.
   - If needed, repeat with the runner-up decomposition scope.
   - Never substitute the legacy checkpoint, relax the MSE gate, or proceed to recording while the gate fails.

Store commands, resolved config, environment versions, metrics, logs, plots, checkpoints, and candidate ranking in the run directory.

## Video validation and final evidence

After the accuracy gate passes:

1. Snapshot the existing MP4 list so older videos cannot satisfy completion.
2. Run the new all-robot wrapper for the 16 robots listed in `record_robots`.
3. Capture one new MP4 per robot directly in `experiments/videos`.
4. Validate each video with OpenCV's FFmpeg-enabled backend:
   - File opens successfully and is nonempty.
   - Width, height, FPS, and frame count are positive.
   - Duration is approximately the configured 10 seconds.
   - First, middle, and final frames decode and are not blank.
5. Build contact sheets from those frames and visually check that the intended robot is visible and rendering is not frozen or corrupted.
6. Require recording logs to contain no traceback, NaN/Inf action, checkpoint mismatch, or fallback-to-teacher message.
7. Write a manifest containing the run ID, checkpoint hash/path, resolved quantization configuration, accepted validation metrics, all 16 robot names and MP4 paths, frame metadata, and validation status.

Do not overwrite or delete existing Lava tutorial changes, checkpoints, experiment runs, or videos. Do not mark the goal complete after implementation, tests, or training alone; completion occurs only after all 16 validated videos and the manifest are present.

## Final completion checklist

- [x] Lava-DL focused and full test suites passed, with saved logs.
- [x] Project focused and full test suites passed, with saved logs.
- [x] Final 24/8 checkpoint metadata, diagnostics, and reproducible training command are saved.
- [x] Final full-validation MSE is `<= 0.00592308546602726`; output is finite and saturation count is zero.
- [x] Trainer and rollout-loader predictions satisfy `atol=1e-6` parity on the fixed validation batch.
- [x] Manifest records one newly generated, OpenCV-validated MP4 for each of the 16 configured robots.
- [x] Contact sheets and recording logs show no rendering failure, traceback, NaN/Inf action, checkpoint mismatch, or teacher fallback.
- [x] All items in the ordered TODO checklist are checked.

## Progress log

### 2026-07-22 — Gate 0 complete

- Created run ID `20260722T053851Z` and initialized `snn/experiments/snn_v2_quantrick/20260722T053851Z/run_manifest.json`.
- Recorded project baseline: branch `main`, commit `f26e467f755b0cf277e1c3845c36cf75a8e9401d`, clean porcelain status.
- Recorded Lava-DL baseline: branch `ann_sdnn`, commit `16241fd9b4ed67124413405504c94a4d28af335b`; four pre-existing modified MNIST tutorial artifacts are explicitly preserved.
- Snapshotted the three pre-existing MP4 filenames in `experiments/videos`; all are marked ineligible as new completion evidence.
- Copied the long-best checkpoint path, epoch 290 MSE `0.00592308546602726`, comparison percentage-error baselines, and resolved long-run configuration (including command-line overrides) into the manifest.
- Next: implement the Lava-DL decomposition API and focused tests without touching the pre-existing tutorial changes.


### 2026-07-22 — Gates 1–2 complete

- Added public `SignMode`, `WeightChunk`, `WeightDecomposer`, and `DecomposedWeightQuantizer` APIs under `lava.lib.dl.slayer.utils`.
- Implemented 24/8 mixed signed-MSB decomposition, excitatory unsigned decomposition, inhibitory magnitude decomposition, exact `int64` reconstruction, clamp/raise saturation policies, diagnostics, NumPy/PyTorch support, and straight-through gradients.
- Preserved the existing quantization path unchanged. Decomposed pre-hooks now reject legacy HDF5 export with an actionable `legacy_8bit` message.
- Added golden-vector, randomized reconstruction, quantization-error, saturation, validation, NumPy/PyTorch parity, dtype/device, STE, Dense/Affine parity, and HDF5 rejection coverage.
- Focused result: `34 passed, 1 skipped in 3.54s`; CUDA preservation is the single skip because CUDA is unavailable.
- Full Lava-DL result: `136 passed, 6 skipped, 2 warnings in 109.47s`.
- The initial full run found Git LFS pointer stubs. Four exact fixtures were hydrated from the repository media endpoint after matching pointer hashes and sizes; the authoritative full rerun then passed.
- Saved logs and environment versions under `snn/experiments/snn_v2_quantrick/20260722T053851Z` and recorded their SHA-256 hashes in `run_manifest.json`.
- Pytest was invoked with `-o addopts=''` because the configured `pytest-cov` plugin is absent; this changes coverage collection only, not test selection.
- Next: integrate quantization configuration, layer-scope wiring, diagnostics, checkpoint metadata, rollout loading, presets, and recording wrapper in SNNv2.


### 2026-07-22 — Gates 3–4 complete

- Added backward-compatible `model.weight_quantization` config parsing/CLI overrides and explicit legacy blocks in the existing default/long YAMLs.
- Added `snn/train_v2_config_quantrick.yaml` with the specified long-best architecture/dynamics, 300 epochs, 24/8 mixed decomposition, deterministic seeds, scheduler, and disabled HDF5 export.
- Trainer scope `all` or `first` now installs decomposed hooks only on Dense/Affine synapses across ANN, sampler, and SNN modes; the scalar input transform is unchanged.
- Trainer validation rejects unsupported modes/signs/scopes/widths and decomposed HDF5 export before dataset loading.
- Added initialization/full-validation/final per-layer diagnostics, finite output/loss/gradient checks, gradient-norm history, zero-saturation enforcement, and metadata checkpoint payloads. Final reload accepts both raw and metadata checkpoints.
- Propagated quantization through `BootstrapTrainingConfig`, `BootstrapStudentPolicy`, raw/metadata rollout loading, PPO defaults/construction, and experiment presets. Checkpoint metadata takes precedence over explicit raw-checkpoint compatibility values.
- Added `record_snnv2_quantrick` and executable `validate/record_snnv2_quantrick_all_robots.sh`; the preset points at the stable `accepted/quantrick_network.pt` target populated only after accuracy and rollout-parity acceptance.
- Fixed the pre-existing `train_dagger_snnv2_longbest` preset mismatch by restoring `algorithm.dagger_online: 50`, matching its committed test contract.
- Focused project result: 25 passed with one deprecation warning. Full project result: 46 passed with the same warning.
- Saved project logs, environment versions, suite selection, and SHA-256 hashes in the run directory and manifest.
- Next: execute the required two-epoch, 4096-sample decomposed smoke run and verify finite metrics, nonzero gradients, and zero saturation.

### 2026-07-22 — Gate 5 complete

- Ran the required two-epoch decomposed smoke job on CPU with 4,096 training and 4,096 validation samples, 20 timesteps, batch size 1,024, seeds 0/1, and all-layer 24/8 mixed quantization.
- Train SNN MSE decreased from `0.1689527873` to `0.0764902364`; full-validation MSE decreased from `0.0860962290` to `0.0630032038`.
- Mean gradient norms were finite and nonzero: `0.1424167963` and `0.0666485252`.
- All six weighted layers had decomposed hooks, finite raw/quantized weights, exact integer reconstruction, and zero saturation at initialization, both full-validation epochs, and final checkpoint reload.
- The epoch-2 metadata checkpoint is self-describing and records decomposed 24/8 mixed all-layer quantization.
- Saved the command, metrics, diagnostics, log, checkpoint, history, plot, and SHA-256 hashes in the run manifest.
- Next: run matched 40-epoch/50k-train/10k-validation legacy, decomposed-first, and decomposed-all candidates.

### 2026-07-22 — Gate 6 in progress

- Launched the matched legacy 8-bit control at `2026-07-22T06:51:15Z` with the exact 40 epochs, 50,000 training samples, 10,000 validation samples, seeds 0/1, 20 timesteps, batch size 1,024, and learning rate `2e-3`.
- CUDA is unavailable; the trial is running on CPU and was healthy at progress capture (PID 80861, approximately 10 CPU cores active).
- Initial legacy diagnostics were finite. The legacy first layer visibly clamps its raw approximately `[-40.64, 40.64]` range to the existing 8-bit approximately `[-4.0, 3.96875]` behavior, while decomposed trials remain pending.
- Epoch 1 completed at `2026-07-22T07:23Z`: train SNN MSE `0.044496554`, full-validation SNN MSE `0.024170106`, full-validation mean/median percentage errors `27.7751%`/`21.2940%`, learning rate `2e-3`, and output absolute mean `0.300789`. Diagnostics remained finite and the best checkpoint was saved; the process continued into epoch 2.
- Epoch 2 completed at `2026-07-22T07:53:15Z`: train SNN MSE fell to `0.021216766`, validation-subset MSE to `0.019172335`, train mean/median percentage errors to `26.3086%`/`20.5004%`, and validation mean/median errors to `24.7710%`/`19.2335%`. Loss/output remained finite at learning rate `2e-3`; no full-validation pass was scheduled for this epoch.
- Epoch 3 completed at `2026-07-22T08:22:42Z`: train SNN MSE improved to `0.017422630`, validation-subset MSE to `0.017012225`, train mean/median percentage errors to `23.9007%`/`18.4977%`, and validation mean/median errors to `23.3284%`/`17.5920%`. Loss/output remained finite at learning rate `2e-3`; no full-validation pass was scheduled for this epoch.
- Epochs 4–9 remained finite and improved monotonically: train MSE moved from `0.015561151` to `0.011405756`, while validation-subset MSE moved from `0.016409450` to `0.013555042`. The manifest records the complete per-epoch MSE, percentage-error, activity, and learning-rate arrays.
- Epoch 10 completed its scheduled full validation and saved a new best checkpoint: full-validation MSE `0.013183315`, mean/median percentage errors `20.5968%`/`15.5943%`, versus the prior best MSE `0.024170106`. Diagnostics remained finite and preserved the legacy first-layer clamp from raw approximately `[-40.64, 40.64]` to quantized `[-4.0, 3.96875]`; the checkpoint SHA-256 is recorded in the manifest.
- Epoch 11 completed with train MSE `0.010646467`, validation-subset MSE `0.012982157`, train mean/median errors `19.3286%`/`15.5887%`, and validation mean/median errors `20.5778%`/`15.5255%`. Loss/output remained finite at learning rate `2e-3`; no full validation was scheduled.
- Epoch 12 completed at `2026-07-22T12:39:47Z` with train MSE `0.010374238`, validation-subset MSE `0.013253118`, train mean/median errors `19.1491%`/`15.5442%`, and validation mean/median errors `20.7019%`/`15.6970%`. The slight subset-validation fluctuation remained finite; the epoch-10 full-validation best `0.013183315` is unchanged.
- The exact command, output directory, log path, and pending trial states are recorded in `run_manifest.json`.

- Added and syntax-checked `dev/codex/scripts/run_quantrick_remaining_candidates.sh`. Orchestrator PID 84612/session 1742 now waits for the exact legacy process, requires its readable history, then automatically runs decomposed-first followed by decomposed-all with identical matched settings and separate logs.

- Added and compiled `dev/codex/scripts/analyze_quantrick_candidates.py`; it requires complete histories/checkpoints, verifies finite metrics/weights and zero saturation, hashes artifacts, and ranks decomposed scopes by MSE, then median percentage error, then mean percentage error. The sequential runner invokes it after both decomposed trials.

- Added and validated `dev/codex/scripts/check_quantrick_acceptance.py`. An exercise against the smoke artifacts correctly rejected MSE `0.0630032038` against the hard `0.00592308546602726` gate while independently confirming decomposed 24/8 metadata, six selected synapses, finite metrics/weights, zero saturation, and artifact hashes.
- Added and syntax-checked `dev/codex/scripts/run_quantrick_final_sequence.sh`. Watcher PID 86152/session 31291 waits for the matched ranking, then automatically runs the winning scope for 300 epochs on the complete dataset. Only if the hard gate fails does it run the specified 100-epoch, `2e-4` long-best fine-tunes for the winning and then runner-up scopes, checking acceptance after each result. The idle watcher was restarted without interrupting legacy training, guaranteeing the live chain includes ranking, acceptance, fallbacks, parity, accepted-artifact promotion, and bundle verification.
- Fixed fallback recording provenance: only after a candidate passes both the hard accuracy gate and real rollout parity, the runner creates a new `accepted/` directory and copies the winning checkpoint/history plus acceptance/parity evidence there. The recording preset now points at `accepted/quantrick_network.pt`, so a successful fallback cannot accidentally record the failed 300-epoch checkpoint; source artifacts are never overwritten.
- Added `dev/codex/scripts/verify_quantrick_accepted_bundle.py` and invoke it both immediately after promotion and before recording. It requires accepted accuracy and parity booleans, matching decomposed 24/8 metadata, zero saturation, parity within tolerance, an allowed source label, and checkpoint/history hashes matching the evidence. Its focused test passes a valid fallback bundle and rejects checkpoint tampering.
- Strengthened the all-robot recording wrapper to snapshot pre-existing MP4s, capture one log per robot, require exactly one newly named MP4 per invocation, and write an authoritative robot-to-video TSV. Shell syntax and all 11 QuanTrick integration tests pass; recording remains gated on final checkpoint acceptance.
- Added `dev/codex/scripts/validate_quantrick_videos.py` for FFmpeg-backed OpenCV metadata/frame validation, duration checks, blank/frozen sampling, forbidden-log scans, hashes, and per-robot contact sheets. OpenCV 4.9.0 reports FFmpeg enabled, and an end-to-end synthetic test validated 16 uniquely mapped videos and generated all 16 contact sheets.
- Chained the validator into the all-robot wrapper: after all 16 recordings it must write `recording/video_validation.json`, generate the contact sheets, and pass every OpenCV/log gate before returning success.
- Added `dev/codex/scripts/verify_quantrick_rollout_parity.py`. On a shape-correct fixture it reconstructed trainer and rollout policies from the smoke metadata checkpoint with exact `0.0` maximum error, finite outputs, matching decomposed hook indices `[0,1,2,3,4,5]`, and zero saturation. The real 256-sample deterministic validation run at `atol=1e-6` is chained immediately after final acceptance; the watcher was restarted so its live process uses this version.
- Reran the entire project suite after adding tamper-resistant bundle verification: `48 passed` with one existing deprecation warning. Saved and hashed `project_full_tests_after_bundle_verification.log`, so the current worktree—not only the earlier integration revision—is covered.
- Dry-ran the prescribed long-best fallback checkpoint through exact initialization under both possible winning scopes. Its raw `OrderedDict` contains six finite bootstrap weights; both scopes copied all six with zero skips, selecting hook `[0]` for `first` and `[0,1,2,3,4,5]` for `all`.
- Verified multi-stage resource headroom while epoch 3 runs: approximately `1.3 TiB` disk and `51 GiB` memory remain available, swap is unused, the current run occupies `43 MiB`, and the legacy process is healthy at roughly `3.0 GiB` RSS with 24 threads and 8 open descriptors.

### 2026-07-22 — Gate 6 CUDA restart

- Paused the original CPU legacy trainer and both downstream orchestration chains with `SIGSTOP` at the user request before CUDA repair. The CPU run completed epoch 13, but its best checkpoint was the epoch-10 model-only checkpoint: it did not contain optimizer or scheduler state, so an exact continuation across the required Docker restart was not possible.
- After the container restart, verified the repaired CUDA stack with NVIDIA driver `595.71.05`, an RTX 4090, PyTorch `2.2.1+cu121`, `torch.cuda.is_available() == True`, and a successful CUDA matrix multiplication.
- Changed both queued launch scripts to request `runtime.device=cuda` instead of CPU and syntax-checked them.
- Cleanly restarted the matched 40-epoch legacy candidate from ANN initialization on CUDA at approximately `2026-07-22T12:55Z`. The live trainer is PID 4066 and reports `device: cuda`; GPU memory use was approximately `3.1 GiB` at the latest capture.
- Restored the remaining-candidate orchestrator (PID 4405), which waits for legacy and then runs decomposed-first and decomposed-all on CUDA, and the final-sequence orchestrator (PID 4770), which waits for candidate ranking before the full/fallback acceptance chain.
- At `2026-07-22T12:58:24Z`, the clean CUDA rerun had completed epoch 26/40 without CUDA errors. The epoch-20 full-validation best was MSE `0.012385791`, mean/median percentage errors `19.9633%`/`15.0049%`, with finite diagnostics and the best checkpoint saved; epoch 26 validation-subset MSE was `0.012430348`.

- The clean CUDA legacy rerun completed all 40/40 epochs at approximately `2026-07-22T13:00Z` without CUDA errors. Final full-validation MSE was `0.011601324`; the best full-validation checkpoint was saved at epoch 40 with mean/median percentage errors `19.3477%`/`14.5056%`, finite diagnostics, and zero saturation.
- The first queue restart revealed an incorrect `REPO_ROOT` calculation in both orchestration scripts: they resolved to the repository `dev` directory and exited before launching candidates. Corrected both scripts from `../../` to `../../../`, syntax-checked them, and relaunched the candidate orchestrator.
- The corrected candidate orchestrator is PID 8768 and has launched `decomposed_first` as PID 8773 on CUDA. The final-sequence orchestrator was relaunched to wait on PID 8768 and is active.

- At `2026-07-22T13:02:02Z`, the corrected `decomposed_first` CUDA candidate had reached epoch 17/40 with finite diagnostics and no saturation. Its epoch-10 full-validation MSE was `0.013840343`; the process remained active as PID 8773 with approximately `2.8 GiB` GPU memory.

- At `2026-07-22T13:02:52Z`, `decomposed_first` reached epoch 30/40 on CUDA with finite diagnostics and zero saturation. Its best full-validation MSE improved to `0.011736688` (epoch 30), with mean/median percentage errors `19.4670%`/`14.6145%`; PID 8773 remained active.

- `decomposed_first` completed all 40/40 CUDA epochs at approximately `2026-07-22T13:03Z`. Its final/best full-validation MSE was `0.011198410`, with mean/median percentage errors `18.8661%`/`14.2408%`; selected-layer diagnostics were finite with zero saturation.
- The queue immediately launched `decomposed_all` on CUDA as PID 9793. Its initialization selected all six weighted layers and reported finite 24/8 reconstruction diagnostics; the final-sequence watcher remains active and waiting for ranking.

- At `2026-07-22T13:04:28Z`, `decomposed_all` was active on CUDA as PID 9793 and had reached epoch 15/40. Its epoch-10 full-validation MSE was `0.012049205`, with finite all-six-layer diagnostics and zero saturation; candidate ranking and the final watcher were still pending.

- At `2026-07-22T13:05:41Z`, `decomposed_all` had reached epoch 35/40 on CUDA with finite all-layer diagnostics and zero saturation. Its best full-validation MSE remained `0.010131364` from epoch 30; PID 9793 remained active and ranking had not yet started.

- `decomposed_all` completed 40/40 CUDA epochs at approximately `2026-07-22T13:06Z`. Its best full-validation MSE was `0.009909998`, with mean/median percentage errors `17.2357%`/`12.1943%`; all six selected-layer diagnostics were finite with zero saturation.
- Candidate ranking was written to `candidate_ranking.json`: `decomposed_all` ranked first and `decomposed_first` runner-up by full-validation MSE, so `decomposed_all` was selected. The corrected final watcher launched the 300-epoch full-dataset run on CUDA as PID 9970 (320,000 train samples; 80,000 validation samples; 20,000 per-epoch validation subset).
- The old shallow-root error remains only as historical text in the first failed queue attempt; the active corrected watcher resolved the repository root correctly and launched the final run from the actual `/app/one_policy_to_run_them_all` tree.

- At `2026-07-22T13:07:35Z`, the selected `decomposed_all` full run was active on CUDA as PID 9970 and had completed epoch 3/300. Epoch 1 full-validation MSE was `0.013041360`; epochs 2–3 remained finite with zero-saturation diagnostics, and GPU memory use was approximately `2.8 GiB`.

- At `2026-07-22T13:08:44Z`, the selected full CUDA run (PID 9970) had completed epoch 6/300. Epoch-1 full-validation MSE was `0.013041360`; epoch-6 subset MSE was `0.008459446`. All observed outputs, gradients, and quantization diagnostics remained finite with zero saturation.

- At `2026-07-22T13:09:08Z`, the selected full CUDA run (PID 9970) reached epoch 7/300. Training remained active and finite; no acceptance or fallback decision has been made yet.

- At `2026-07-22T13:09:41Z`, the selected full CUDA run (PID 9970) reached epoch 8/300. The run remained active with finite metrics and no acceptance/fallback artifacts yet; the historical shallow-root message in the watcher log is from the earlier failed launch and does not describe the active corrected run.

- At `2026-07-22T13:10:04Z`, the selected full CUDA run (PID 9970) reached epoch 9/300. It remained active with finite outputs and quantization diagnostics; no acceptance, fallback, parity, or recording artifacts had been created yet.

- At `2026-07-22T13:10:25Z`, the selected full CUDA run (PID 9970) completed epoch 10/300 and saved a new best full-validation MSE of `0.008489255`, with full-validation mean/median percentage errors `16.0722%`/`11.4371%`. All six-layer diagnostics remained finite with zero saturation; the acceptance chain has not started because training is still running.

- At `2026-07-22T13:11:31Z`, the selected full CUDA run (PID 9970) reached epoch 13/300. The epoch-10 best full-validation MSE remained `0.008489255`; epoch-13 subset MSE was `0.007678576`, with finite metrics and no saturation.

- At `2026-07-22T13:11:57Z`, the selected full CUDA run (PID 9970) reached epoch 14/300. Best full-validation MSE remained `0.008489255`; epoch-14 subset MSE was `0.007701326`, with finite metrics and zero saturation.

- At `2026-07-22T13:12:22Z`, the selected full CUDA run (PID 9970) reached epoch 16/300. Best full-validation MSE remained `0.008489255`; epoch-16 subset MSE was `0.007481129`, with finite metrics and zero saturation.

- At `2026-07-22T13:12:50Z`, the selected full CUDA run (PID 9970) reached epoch 17/300. Best full-validation MSE remained `0.008489255`; epoch-17 subset MSE improved to `0.007257177`, with finite metrics and zero saturation.

- At `2026-07-22T13:13:24Z`, the selected full CUDA run (PID 9970) reached epoch 18/300. Best full-validation MSE remained `0.008489255`; epoch-18 subset MSE was `0.007278485`, with finite metrics and zero saturation.

- At `2026-07-22T13:13:50Z`, the selected full CUDA run (PID 9970) reached epoch 19/300. Best full-validation MSE remained `0.008489255`; epoch-19 subset MSE improved to `0.007045467`, with finite metrics and zero saturation.

- At `2026-07-22T13:14:23Z`, the selected full CUDA run (PID 9970) completed epoch 21/300. Epoch 20 saved a new best full-validation MSE of `0.007149102`, with full-validation mean/median percentage errors `14.8963%`/`10.7849%`; epoch-21 subset MSE was `0.007134214`. Diagnostics remain finite with zero saturation.

- At `2026-07-22T13:15:01Z`, the selected full CUDA run (PID 9970) reached epoch 23/300. Best full-validation MSE remained `0.007149102`; epoch-23 subset MSE was `0.006989746`, with finite metrics and zero saturation.

- At `2026-07-22T13:15:29Z`, the selected full CUDA run (PID 9970) reached epoch 24/300. Best full-validation MSE remained `0.007149102`; epoch-24 subset MSE improved to `0.006892948`, with finite metrics and zero saturation.

- At `2026-07-22T13:15:57Z`, the selected full CUDA run (PID 9970) reached epoch 25/300. Best full-validation MSE remained `0.007149102`; epoch-25 subset MSE improved to `0.006680147`, with finite metrics and zero saturation.

- At `2026-07-22T13:16:34Z`, the selected full CUDA run (PID 9970) reached epoch 27/300. Best full-validation MSE remained `0.007149102`; epoch-27 subset MSE was `0.006726247`, with finite metrics and zero saturation.

- At `2026-07-22T13:17:00Z`, the selected full CUDA run (PID 9970) reached epoch 28/300. Best full-validation MSE remained `0.007149102`; epoch-28 subset MSE was `0.006742285`, with finite metrics and zero saturation.

- At `2026-07-22T13:17:43Z`, the selected full CUDA run (PID 9970) completed epoch 30/300 and saved a new best full-validation MSE of `0.006665716`, with full-validation mean/median percentage errors `14.3416%`/`10.3605%`. All six-layer diagnostics remained finite with zero saturation; the hard gate `0.00592308546602726` has not yet been reached.

- At `2026-07-22T13:18:24Z`, the selected full CUDA run (PID 9970) reached epoch 32/300. Best full-validation MSE remained `0.006665716`; epoch-32 subset MSE improved to `0.006497732`, with finite metrics and zero saturation.
- At `2026-07-22T13:19:33Z`, the selected full CUDA run (PID 9970) reached epoch 35/300. Best full-validation MSE remained `0.006665716`; epoch-35 subset MSE was `0.006499443`, with finite metrics and zero saturation. The hard gate `0.00592308546602726` remains unmet, so downstream acceptance/parity has not started.
- At `2026-07-22T13:20:15Z`, the selected full CUDA run (PID 9970) reached epoch 37/300. Best full-validation MSE remained `0.006665716`; epoch-37 subset MSE improved to `0.006312032`, with finite metrics and zero saturation. The hard gate `0.00592308546602726` remains unmet.
- At `2026-07-22T13:21:30Z`, the selected full CUDA run (PID 9970) completed epoch 40/300 and saved a new best full-validation MSE of `0.006224059`, with full-validation mean/median percentage errors `13.7435%`/`9.8891%`. All six-layer diagnostics remained finite with zero saturation; the hard gate `0.00592308546602726` remains unmet and the acceptance chain has not started.
- At `2026-07-22T13:21:48Z`, the selected full CUDA run (PID 9970) reached epoch 41/300. Best full-validation MSE remained `0.006224059`; epoch-41 subset MSE was `0.006282428`, with finite metrics and zero saturation. The hard gate `0.00592308546602726` remains unmet.
- At `2026-07-22T13:22:10Z`, the selected full CUDA run (PID 9970) reached epoch 42/300. Best full-validation MSE remained `0.006224059`; epoch-42 subset MSE was `0.006292723`, with finite metrics and zero saturation. The hard gate `0.00592308546602726` remains unmet and no acceptance/fallback artifacts exist.
- At `2026-07-22T13:22:52Z`, the selected full CUDA run (PID 9970) reached epoch 43/300. Best full-validation MSE remained `0.006224059`; epoch-43 subset MSE was `0.006412070`, with finite metrics and zero saturation. The hard gate `0.00592308546602726` remains unmet.
- At `2026-07-22T13:23:15Z`, the selected full CUDA run (PID 9970) reached epoch 44/300. Best full-validation MSE remained `0.006224059`; epoch-44 subset MSE regressed to `0.013360648`, but metrics stayed finite with zero saturation. The hard gate `0.00592308546602726` remains unmet.
- At `2026-07-22T13:23:42Z`, the selected full CUDA run (PID 9970) reached epoch 46/300. Best full-validation MSE remained `0.006224059`; epoch-46 subset MSE was `0.007986136` after the earlier finite transient regression, with zero saturation. The hard gate `0.00592308546602726` remains unmet.
- At `2026-07-22T13:24:05Z`, the selected full CUDA run (PID 9970) reached epoch 47/300. Best full-validation MSE remained `0.006224059`; epoch-47 subset MSE improved from the transient regression to `0.007425175`, with finite metrics and zero saturation. The hard gate `0.00592308546602726` remains unmet.
- At `2026-07-22T13:24:28Z`, the selected full CUDA run (PID 9970) reached epoch 48/300. Best full-validation MSE remained `0.006224059`; epoch-48 subset MSE improved to `0.007139298`, with finite metrics and zero saturation. The hard gate `0.00592308546602726` remains unmet.
- At `2026-07-22T13:24:52Z`, the selected full CUDA run (PID 9970) reached epoch 49/300. Best full-validation MSE remained `0.006224059`; epoch-49 subset MSE improved to `0.006970150`, with finite metrics and zero saturation. The hard gate `0.00592308546602726` remains unmet.
- At `2026-07-22T13:25:15Z`, the selected full CUDA run (PID 9970) completed epoch 50/300. The scheduled full-validation MSE was `0.006924857`; the best full-validation MSE remains `0.006224059` (epoch 40). Diagnostics remained finite with zero saturation, and the hard gate `0.00592308546602726` remains unmet.
- At `2026-07-22T13:25:42Z`, the selected full CUDA run (PID 9970) reached epoch 51/300. Best full-validation MSE remained `0.006224059`; epoch-51 subset MSE improved to `0.006741282`. The plateau scheduler reduced learning rate to `1.40e-03`; metrics remained finite with zero saturation and the hard gate remains unmet.
- At `2026-07-22T13:26:02Z`, the selected full CUDA run (PID 9970) reached epoch 52/300. Best full-validation MSE remained `0.006224059`; epoch-52 subset MSE improved to `0.006316787` at learning rate `1.40e-03`, with finite metrics and zero saturation. The hard gate remains unmet.
- At `2026-07-22T13:26:25Z`, the selected full CUDA run (PID 9970) reached epoch 53/300. Best full-validation MSE remained `0.006224059`; epoch-53 subset MSE improved to `0.006187766` at learning rate `1.40e-03`, with finite metrics and zero saturation. The hard gate remains unmet.
- At `2026-07-22T13:26:52Z`, the selected full CUDA run (PID 9970) reached epoch 54/300. Best full-validation MSE remained `0.006224059`; epoch-54 subset MSE was `0.006338678` at learning rate `1.40e-03`, with finite metrics and zero saturation. The hard gate remains unmet.
- At `2026-07-22T13:27:16Z`, the selected full CUDA run (PID 9970) reached epoch 55/300. Best full-validation MSE remained `0.006224059`; epoch-55 subset MSE was `0.006275367` at learning rate `1.40e-03`, with finite metrics and zero saturation. The hard gate remains unmet.
- At `2026-07-22T13:27:44Z`, the selected full CUDA run (PID 9970) reached epoch 56/300. Best full-validation MSE remained `0.006224059`; epoch-56 subset MSE improved to `0.006193957` at learning rate `1.40e-03`, with finite metrics and zero saturation. The hard gate remains unmet.
- At `2026-07-22T13:28:16Z`, the selected full CUDA run (PID 9970) reached epoch 58/300. Best full-validation MSE remained `0.006224059`; epoch-58 subset MSE improved to `0.006102389` at learning rate `1.40e-03`, with finite metrics and zero saturation. The hard gate remains unmet.
- At `2026-07-22T13:28:48Z`, the selected full CUDA run (PID 9970) reached epoch 59/300. Best full-validation MSE remained `0.006224059`; epoch-59 subset MSE improved to `0.006077373` at learning rate `1.40e-03`, with finite metrics and zero saturation. The hard gate remains unmet.
- At `2026-07-22T13:29:23Z`, the selected full CUDA run (PID 9970) completed epoch 60 and reached epoch 61/300. Epoch 60 saved a new best full-validation MSE of `0.005925232` (mean/median percentage errors `13.3391%`/`9.5594%`), only `0.0000021465` above the hard gate `0.00592308546602726`; epoch-61 subset MSE was `0.006008438`. Metrics remained finite with zero saturation, and acceptance has not started.
- At `2026-07-22T13:29:55Z`, the selected full CUDA run (PID 9970) reached epoch 62/300. Best full-validation MSE remained `0.005925232`; epoch-62 subset MSE improved to `0.005857558` at learning rate `1.40e-03`, with finite metrics and zero saturation. The hard gate remains narrowly unmet.
- At `2026-07-22T13:30:34Z`, the selected full CUDA run (PID 9970) reached epoch 64/300. Best full-validation MSE remained `0.005925232`; epoch-64 subset MSE improved to `0.005853649` at learning rate `1.40e-03`, with finite metrics and zero saturation. The hard gate remains narrowly unmet and acceptance has not started.
- At `2026-07-22T13:31:17Z`, the selected full CUDA run (PID 9970) reached epoch 66/300. Best full-validation MSE remained `0.005925232`; epoch-66 subset MSE improved to `0.005836491` at learning rate `1.40e-03`, with finite metrics and zero saturation. The hard gate remains narrowly unmet and acceptance has not started.
- At `2026-07-22T13:31:52Z`, the selected full CUDA run (PID 9970) reached epoch 67/300. Best full-validation MSE remained `0.005925232`; epoch-67 subset MSE was `0.005845629` at learning rate `1.40e-03`, with finite metrics and zero saturation. The hard gate remains narrowly unmet and acceptance has not started.
- At `2026-07-22T13:32:24Z`, the selected full CUDA run (PID 9970) reached epoch 69/300. Best full-validation MSE remained `0.005925232`; epoch-69 subset MSE was `0.005910929` at learning rate `1.40e-03`, with finite metrics and zero saturation. The hard gate remains narrowly unmet and acceptance has not started.
- At `2026-07-22T13:33:00Z`, the selected full CUDA run (PID 9970) completed epoch 70 and reached the hard gate: full-validation MSE improved to `0.005819064`, below `0.00592308546602726`, with full-validation mean/median percentage errors `13.3370%`/`9.5544%`. Diagnostics remained finite with zero saturation; the 300-epoch run is still active and acceptance/parity have not yet started.
- At `2026-07-22T13:33:31Z`, the selected full CUDA run (PID 9970) reached epoch 72/300. Best full-validation MSE remained `0.005819064`; epoch-72 subset MSE was `0.005857155` at learning rate `1.40e-03`, with finite metrics and zero saturation. The 300-epoch run remains active; acceptance/parity have not started.
- At `2026-07-22T13:34:03Z`, the selected full CUDA run (PID 9970) reached epoch 73/300. Best full-validation MSE remained `0.005819064`; epoch-73 subset MSE was `0.005840975` at learning rate `1.40e-03`, with finite metrics and zero saturation. The 300-epoch run remains active; acceptance/parity have not started.
- At `2026-07-22T13:34:38Z`, the selected full CUDA run (PID 9970) reached epoch 75/300. Best full-validation MSE remained `0.005819064`; epoch-75 subset MSE improved to `0.005704752` at learning rate `1.40e-03`, with finite metrics and zero saturation. The 300-epoch run remains active; acceptance/parity have not started.
- At `2026-07-22T13:35:18Z`, the selected full CUDA run (PID 9970) reached epoch 77/300. Best full-validation MSE remained `0.005819064`; epoch-77 subset MSE was `0.005746449` at learning rate `1.40e-03`, with finite metrics and zero saturation. The 300-epoch run remains active; acceptance/parity have not started.
- At `2026-07-22T13:35:50Z`, the selected full CUDA run (PID 9970) reached epoch 78/300. Best full-validation MSE remained `0.005819064`; epoch-78 subset MSE was `0.005722050` at learning rate `1.40e-03`, with finite metrics and zero saturation. The 300-epoch run remains active; acceptance/parity have not started.
- At `2026-07-22T13:36:20Z`, the selected full CUDA run (PID 9970) reached epoch 79/300. Best full-validation MSE remained `0.005819064`; epoch-79 subset MSE improved to `0.005657218` at learning rate `1.40e-03`, with finite metrics and zero saturation. The 300-epoch run remains active; acceptance/parity have not started.
- At `2026-07-22T13:36:58Z`, the selected full CUDA run (PID 9970) completed epoch 80 and reached epoch 81/300. Epoch 80 saved a new best full-validation MSE of `0.005657950` (mean/median percentage errors `13.1131%`/`9.5070%`); epoch-81 subset MSE was `0.005618983`. Metrics remained finite with zero saturation, and the 300-epoch run remains active.
- At `2026-07-22T13:37:34Z`, the selected full CUDA run (PID 9970) reached epoch 83/300. Best full-validation MSE remained `0.005657950`; epoch-83 subset MSE was `0.005634697` at learning rate `1.40e-03`, with finite metrics and zero saturation. The 300-epoch run remains active; acceptance/parity have not started.
- At `2026-07-22T13:38:17Z`, the selected full CUDA run (PID 9970) reached epoch 84/300. Best full-validation MSE remained `0.005657950`; epoch-84 subset MSE was `0.005721965` at learning rate `1.40e-03`, with finite metrics and zero saturation. The 300-epoch run remains active; acceptance/parity have not started.
- At `2026-07-22T13:38:52Z`, the selected full CUDA run (PID 9970) reached epoch 86/300. Best full-validation MSE remained `0.005657950`; epoch-86 subset MSE was `0.005696200` at learning rate `1.40e-03`, with finite metrics and zero saturation. The 300-epoch run remains active; acceptance/parity have not started.
- At `2026-07-22T13:39:31Z`, the selected full CUDA run (PID 9970) reached epoch 88/300. Best full-validation MSE remained `0.005657950`; epoch-88 subset MSE was `0.005664381` at learning rate `1.40e-03`, with finite metrics and zero saturation. The 300-epoch run remains active; acceptance/parity have not started.
- At `2026-07-22T13:40:18Z`, the selected full CUDA run (PID 9970) completed epoch 90 and reached epoch 90/300. Epoch 90 saved a new best full-validation MSE of `0.005573586` (mean/median percentage errors `12.9752%`/`9.3366%`), with finite metrics and zero saturation. The 300-epoch run remains active; acceptance/parity have not started.
- At `2026-07-22T13:41:06Z`, the selected full CUDA run (PID 9970) reached epoch 92/300. Best full-validation MSE remained `0.005573586`; epoch-92 subset MSE was `0.005709803` at learning rate `1.40e-03`, with finite metrics and zero saturation. The 300-epoch run remains active; acceptance/parity have not started.
- At `2026-07-22T13:41:47Z`, the selected full CUDA run (PID 9970) reached epoch 94/300. Best full-validation MSE remained `0.005573586`; epoch-94 subset MSE was `0.005648417` at learning rate `1.40e-03`, with finite metrics and zero saturation. The 300-epoch run remains active; acceptance/parity have not started.
- At `2026-07-22T13:42:21Z`, the selected full CUDA run (PID 9970) reached epoch 95/300. Best full-validation MSE remained `0.005573586`; epoch-95 subset MSE was `0.005651238` at learning rate `1.40e-03`, with finite metrics and zero saturation. The 300-epoch run remains active; acceptance/parity have not started.
- At `2026-07-22T13:43:02Z`, the selected full CUDA run (PID 9970) reached epoch 97/300. Best full-validation MSE remained `0.005573586`; epoch-97 subset MSE was `0.005605350` at learning rate `1.40e-03`, with finite metrics and zero saturation. The 300-epoch run remains active; acceptance/parity have not started.
- At `2026-07-22T13:43:24Z`, the selected full CUDA run (PID 9970) reached epoch 98/300. Best full-validation MSE remained `0.005573586`; epoch-98 subset MSE was `0.005597988` at learning rate `1.40e-03`, with finite metrics and zero saturation. The 300-epoch run remains active; acceptance/parity have not started.
- At `2026-07-22T13:44:41Z`, the selected full CUDA run (PID 9970) reached epoch 101/300. Full-validation MSE improved to `0.005498406` at epoch 100 (mean/median `12.8514%/9.3411%`), and epoch-101 subset MSE was `0.005490378` at learning rate `1.40e-03`; metrics remain finite with zero quantization saturation. The 300-epoch run remains active; acceptance/parity have not started.
- At `2026-07-22T13:45:20Z`, the selected full CUDA run (PID 9970) reached epoch 102/300. Best full-validation MSE remains `0.005498406`; epoch-102 subset MSE was `0.005510903` at learning rate `1.40e-03`, with finite metrics and zero quantization saturation. Acceptance/parity have not started, and the 300-epoch run remains active.
- At `2026-07-22T13:45:50Z`, the selected full CUDA run (PID 9970) reached epoch 104/300. Best full-validation MSE remains `0.005498406`; epoch-104 subset MSE was `0.005442952` at learning rate `1.40e-03`, with finite metrics and zero quantization saturation. Acceptance/parity have not started, and the 300-epoch run remains active.
- At `2026-07-22T13:46:19Z`, the selected full CUDA run (PID 9970) reached epoch 105/300. Best full-validation MSE remains `0.005498406`; epoch-105 subset MSE was `0.005457738` at learning rate `1.40e-03`, with finite metrics and zero quantization saturation. Acceptance/parity have not started; the run remains active. The videos directory currently contains 3 MP4s, so the required 16-video deliverable is not yet met.
- At `2026-07-22T13:47:09Z`, the selected full CUDA run (PID 9970) reached epoch 107/300. Best full-validation MSE remains `0.005498406`; epoch-107 subset MSE was `0.005533076` at learning rate `1.40e-03`, with finite metrics and zero quantization saturation. Acceptance/parity have not started; the run remains active and the videos directory still has 3 MP4s (16 required).
- At `2026-07-22T13:47:35Z`, the selected full CUDA run (PID 9970) reached epoch 109/300. Best full-validation MSE remains `0.005498406`; epoch-109 subset MSE was `0.005499908` at learning rate `1.40e-03`, with finite metrics and zero quantization saturation. Acceptance/parity have not started; the run remains active and the videos directory still has 3 MP4s (16 required).
- At `2026-07-22T13:48:05Z`, the selected full CUDA run (PID 9970) reached epoch 110/300. Full-validation MSE improved to `0.005406075` (mean/median `12.7361%/9.2008%`), with epoch-110 subset MSE `0.005441074` at learning rate `1.40e-03`; quantization diagnostics are finite with zero saturation. Acceptance/parity have not started; the run remains active. A direct directory listing confirms 3 pre-existing MP4s, so the required 16 new videos are still outstanding.
- At `2026-07-22T13:48:32Z`, the selected full CUDA run (PID 9970) reached epoch 111/300. Best full-validation MSE remains `0.005406075`; epoch-111 subset MSE was `0.005407210` at learning rate `1.40e-03`, with finite metrics and zero quantization saturation. Acceptance/parity have not started; the run remains active. The run manifest and post-test logs exist, but no acceptance/parity result has been authorized by the active training completion, and only 3 MP4s are present.
- At `2026-07-22T13:48:57Z`, the selected full CUDA run (PID 9970) reached epoch 113/300. Best full-validation MSE remains `0.005406075`; epoch-113 subset MSE was `0.005453829` at learning rate `1.40e-03`, with finite metrics and zero quantization saturation. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T13:49:23Z`, the selected full CUDA run (PID 9970) reached epoch 114/300. Best full-validation MSE remains `0.005406075`; epoch-114 subset MSE was `0.005344180` at learning rate `1.40e-03`, with finite metrics and zero quantization saturation. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T13:49:49Z`, the selected full CUDA run (PID 9970) reached epoch 115/300. Best full-validation MSE remains `0.005406075`; epoch-115 subset MSE was `0.005340513` at learning rate `1.40e-03`, with finite metrics and zero quantization saturation. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T13:50:13Z`, the selected full CUDA run (PID 9970) reached epoch 116/300. Best full-validation MSE remains `0.005406075`; epoch-116 subset MSE was `0.005406328` at learning rate `1.40e-03`, with finite metrics and zero quantization saturation. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T13:50:40Z`, the selected full CUDA run (PID 9970) reached epoch 117/300. Best full-validation MSE remains `0.005406075`; epoch-117 subset MSE was `0.005365443` at learning rate `1.40e-03`, with finite metrics and zero quantization saturation. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T13:51:10Z`, the selected full CUDA run (PID 9970) reached epoch 118/300. Best full-validation MSE remains `0.005406075`; epoch-118 subset MSE was `0.005424275` at learning rate `1.40e-03`, with finite metrics and zero quantization saturation. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T13:51:34Z`, the selected full CUDA run (PID 9970) reached epoch 119/300. Best full-validation MSE remains `0.005406075`; epoch-119 subset MSE was `0.005410899` at learning rate `1.40e-03`, with finite metrics and zero quantization saturation. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T13:52:00Z`, the selected full CUDA run (PID 9970) reached epoch 121/300. Full-validation MSE improved to `0.005301266` at epoch 120 (mean/median `12.5504%/9.0427%`); epoch-121 subset MSE was `0.005414363` at learning rate `1.40e-03`. Quantization diagnostics are finite with zero saturation. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T13:52:28Z`, the selected full CUDA run (PID 9970) reached epoch 122/300. Best full-validation MSE remains `0.005301266`; epoch-122 subset MSE was `0.005357044` at learning rate `1.40e-03`, with finite metrics and zero quantization saturation. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T13:52:56Z`, the selected full CUDA run (PID 9970) reached epoch 123/300. Best full-validation MSE remains `0.005301266`; epoch-123 subset MSE was `0.005396094` at learning rate `1.40e-03`, with finite metrics and zero quantization saturation. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T13:53:23Z`, the selected full CUDA run (PID 9970) reached epoch 124/300. Best full-validation MSE remains `0.005301266`; epoch-124 subset MSE was `0.005306334` at learning rate `1.40e-03`, with finite metrics and zero quantization saturation. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T13:53:50Z`, the selected full CUDA run (PID 9970) reached epoch 125/300. Best full-validation MSE remains `0.005301266`; epoch-125 subset MSE was `0.005362441` at learning rate `1.40e-03`, with finite metrics and zero quantization saturation. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T13:54:19Z`, the selected full CUDA run (PID 9970) reached epoch 126/300. Best full-validation MSE remains `0.005301266`; epoch-126 subset MSE was `0.005337876` at learning rate `1.40e-03`, with finite metrics and zero quantization saturation. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T13:54:53Z`, the selected full CUDA run (PID 9970) reached epoch 128/300. Best full-validation MSE remains `0.005301266`; epoch-128 subset MSE was `0.005325520` at learning rate `1.40e-03`, with finite metrics and zero quantization saturation. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T13:55:37Z`, the selected full CUDA run (PID 9970) reached epoch 130/300. Full-validation MSE at epoch 130 was `0.005308582` (mean/median `12.5438%/9.0775%`), so the best remains `0.005301266`; epoch-130 quantization diagnostics are finite with zero saturation. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T13:56:05Z`, the selected full CUDA run (PID 9970) reached epoch 131/300. Best full-validation MSE remains `0.005301266`; epoch-131 subset MSE was `0.005278065` at learning rate `1.40e-03`, with finite metrics and zero quantization saturation. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T13:56:32Z`, the selected full CUDA run (PID 9970) reached epoch 132/300. Best full-validation MSE remains `0.005301266`; epoch-132 subset MSE was `0.005321055` at learning rate `1.40e-03`, with finite metrics and zero quantization saturation. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T13:57:09Z`, the selected full CUDA run (PID 9970) reached epoch 134/300. Best full-validation MSE remains `0.005301266`; epoch-134 subset MSE was `0.005359774` at learning rate `1.40e-03`, with finite metrics and zero quantization saturation. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T13:57:36Z`, the selected full CUDA run (PID 9970) reached epoch 135/300. Best full-validation MSE remains `0.005301266`; epoch-135 subset MSE was `0.005292752` at learning rate `1.40e-03`, with finite metrics and zero quantization saturation. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T13:58:04Z`, the selected full CUDA run (PID 9970) reached epoch 136/300. Best full-validation MSE remains `0.005301266`; epoch-136 subset MSE was `0.005342804` at learning rate `1.40e-03`, with finite metrics and zero quantization saturation. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T13:58:34Z`, the selected full CUDA run (PID 9970) reached epoch 138/300. Best full-validation MSE remains `0.005301266`; epoch-138 subset MSE was `0.005246372` at learning rate `1.40e-03`, with finite metrics and zero quantization saturation. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T13:59:05Z`, the selected full CUDA run (PID 9970) reached epoch 139/300. Best full-validation MSE remains `0.005301266`; epoch-139 subset MSE was `0.005334770` at learning rate `1.40e-03`, with finite metrics and zero quantization saturation. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T13:59:39Z`, the selected full CUDA run (PID 9970) reached epoch 140/300. Full-validation MSE improved to `0.005261456` (mean/median `12.4895%/8.9970%`); epoch-140 subset MSE was `0.005277110` at learning rate `1.40e-03`. Quantization diagnostics are finite with zero saturation. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:00:06Z`, the selected full CUDA run (PID 9970) reached epoch 141/300. Best full-validation MSE remains `0.005261456`; epoch-141 subset MSE was `0.005323705` at learning rate `1.40e-03`, with finite metrics and zero quantization saturation. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:00:40Z`, the selected full CUDA run (PID 9970) reached epoch 143/300. Best full-validation MSE remains `0.005261456`; epoch-143 subset MSE was `0.005335513` at learning rate `1.40e-03`, with finite metrics and zero quantization saturation. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:01:12Z`, the selected full CUDA run (PID 9970) reached epoch 144/300. Best full-validation MSE remains `0.005261456`; epoch-144 subset MSE was `0.005292134` at learning rate `1.40e-03`, with finite metrics and zero quantization saturation. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:01:45Z`, the selected full CUDA run (PID 9970) reached epoch 146/300. Best full-validation MSE remains `0.005261456`; epoch-146 subset MSE was `0.005221375` at learning rate `1.40e-03`, with finite metrics and zero quantization saturation. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:02:18Z`, the selected full CUDA run (PID 9970) reached epoch 147/300. Best full-validation MSE remains `0.005261456`; epoch-147 subset MSE was `0.005344817` at learning rate `1.40e-03`, with finite metrics and zero quantization saturation. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:02:48Z`, the selected full CUDA run (PID 9970) reached epoch 149/300. Best full-validation MSE remains `0.005261456`; epoch-149 subset MSE was `0.005313314` at learning rate `1.40e-03`, with finite metrics and zero quantization saturation. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:03:14Z`, the selected full CUDA run (PID 9970) reached epoch 150/300. Full-validation MSE improved to `0.005177258` (mean/median `12.3991%/8.9748%`); epoch-150 subset MSE was `0.005196159` at learning rate `1.40e-03`. Quantization diagnostics are finite with zero saturation. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:03:45Z`, the selected full CUDA run (PID 9970) reached epoch 151/300. Best full-validation MSE remains `0.005177258`; epoch-151 subset MSE was `0.005306752` at learning rate `1.40e-03`, with finite metrics and zero quantization saturation. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:04:19Z`, the selected full CUDA run (PID 9970) reached epoch 152/300. Best full-validation MSE remains `0.005177258`; epoch-152 subset MSE was `0.005262439` at learning rate `1.40e-03`, with finite metrics and zero quantization saturation. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:04:52Z`, the selected full CUDA run (PID 9970) reached epoch 154/300. Best full-validation MSE remains `0.005177258`; epoch-154 subset MSE was `0.005224096` at learning rate `1.40e-03`, with finite metrics and zero quantization saturation. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:05:23Z`, the selected full CUDA run (PID 9970) reached epoch 155/300. Best full-validation MSE remains `0.005177258`; epoch-155 subset MSE was `0.005218422` at learning rate `1.40e-03`, with finite metrics and zero quantization saturation. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:05:54Z`, the selected full CUDA run (PID 9970) reached epoch 157/300. Best full-validation MSE remains `0.005177258`; epoch-157 subset MSE was `0.005173784` at learning rate `1.40e-03`, with finite metrics and zero quantization saturation. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:06:26Z`, the selected full CUDA run (PID 9970) reached epoch 158/300. Best full-validation MSE remains `0.005177258`; epoch-158 subset MSE was `0.005149474` at learning rate `1.40e-03`, with finite metrics and zero quantization saturation. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:06:56Z`, the selected full CUDA run (PID 9970) reached epoch 159/300. Best full-validation MSE remains `0.005177258`; epoch-159 subset MSE was `0.005186987` at learning rate `1.40e-03`, with finite metrics and zero quantization saturation. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:07:26Z`, the selected full CUDA run (PID 9970) reached epoch 161/300. Full-validation MSE improved to `0.005168528` (mean/median `12.3681%/8.9580%`) at epoch 160; epoch-161 subset MSE was `0.005273140` at learning rate `1.40e-03`. Quantization diagnostics are finite with zero saturation. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:08:02Z`, the selected full CUDA run (PID 9970) reached epoch 162/300. Best full-validation MSE remains `0.005168528`; epoch-162 subset MSE was `0.005244451` at learning rate `1.40e-03`, with finite metrics and zero quantization saturation. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:08:37Z`, the selected full CUDA run (PID 9970) reached epoch 164/300. Best full-validation MSE remains `0.005168528`; epoch-164 subset MSE was `0.005283934` at learning rate `1.40e-03`, with finite metrics and zero quantization saturation. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:09:09Z`, the selected full CUDA run (PID 9970) reached epoch 165/300. Best full-validation MSE remains `0.005168528`; epoch-165 subset MSE was `0.005226056` at learning rate `1.40e-03`, with finite metrics and zero quantization saturation. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:09:45Z`, the selected full CUDA run (PID 9970) reached epoch 167/300. Best full-validation MSE remains `0.005168528`; epoch-167 subset MSE was `0.005159122` at learning rate `1.40e-03`, with finite metrics and zero quantization saturation. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:10:25Z`, the selected full CUDA run (PID 9970) reached epoch 169/300. Best full-validation MSE remains `0.005168528`; epoch-169 subset MSE was `0.005241834` after the scheduled learning-rate decay to `9.80e-04` (epoch 168 used `1.40e-03`). Metrics remain finite with zero quantization saturation. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:11:01Z`, the selected full CUDA run (PID 9970) reached epoch 170/300. Full-validation MSE improved to `0.004943941` (mean/median `12.0381%/8.7253%`); epoch-170 subset MSE was `0.004978795` at learning rate `9.80e-04`. Quantization diagnostics are finite with zero saturation. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:11:36Z`, the selected full CUDA run (PID 9970) reached epoch 172/300. Best full-validation MSE remains `0.004943941`; epoch-172 subset MSE was `0.004985583` at learning rate `9.80e-04`, with finite metrics and zero quantization saturation. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:12:13Z`, the selected full CUDA run (PID 9970) reached epoch 173/300. Best full-validation MSE remains `0.004943941`; epoch-173 subset MSE was `0.005030368` at learning rate `9.80e-04`, with finite metrics and zero quantization saturation. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:12:46Z`, the selected full CUDA run (PID 9970) reached epoch 175/300. Best full-validation MSE remains `0.004943941`; epoch-175 subset MSE was `0.004975585` at learning rate `9.80e-04`, with finite metrics and zero quantization saturation. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:13:19Z`, the selected full CUDA run (PID 9970) reached epoch 176/300. Best full-validation MSE remains `0.004943941`; epoch-176 subset MSE was `0.005017390` at learning rate `9.80e-04`, with finite metrics and zero quantization saturation. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:13:59Z`, the selected full CUDA run (PID 9970) reached epoch 178/300. Best full-validation MSE remains `0.004943941`; epoch-178 subset MSE was `0.005082404` at learning rate `9.80e-04`, with finite metrics and zero quantization saturation. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:14:34Z`, the selected full CUDA run (PID 9970) reached epoch 179/300. Best full-validation MSE remains `0.004943941`; epoch-179 subset MSE was `0.005068056` at learning rate `9.80e-04`, with finite metrics and zero quantization saturation. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:15:56Z`, the selected full CUDA run (PID 9970) reached epoch 183/300. The latest full-validation result (epoch 180) was MSE `0.005000985` with mean/median `12.0965%/8.7226%`; best remains `0.004943941`. Epoch-183 subset MSE was `0.004956040` at learning rate `6.86e-04`; epoch-180 quantization diagnostics were finite with zero saturation. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:16:24Z`, the selected full CUDA run (PID 9970) reached epoch 185/300. Best full-validation MSE remains `0.004943941`; epoch-185 subset MSE was `0.004932680` at learning rate `6.86e-04`, with finite metrics and no observed quantization saturation (latest diagnostics at epoch 180). Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:17:06Z`, the selected full CUDA run (PID 9970) reached epoch 186/300. Best full-validation MSE remains `0.004943941`; epoch-186 subset MSE was `0.004854945` at learning rate `6.86e-04`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:17:23Z`, the selected full CUDA run (PID 9970) reached epoch 187/300. Best full-validation MSE remains `0.004943941`; epoch-187 subset MSE was `0.004940339` at learning rate `6.86e-04`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:17:42Z`, the selected full CUDA run (PID 9970) reached epoch 188/300. Best full-validation MSE remains `0.004943941`; epoch-188 subset MSE was `0.004950880` at learning rate `6.86e-04`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:18:01Z`, the selected full CUDA run (PID 9970) reached epoch 189/300. Best full-validation MSE remains `0.004943941`; epoch-189 subset MSE was `0.004943934` at learning rate `6.86e-04`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:19:18Z`, the selected full CUDA run (PID 9970) reached epoch 192/300. Epoch 190 produced a new best full-validation MSE of `0.004795069` (mean/median `11.7805%/8.4154%`), below the `0.00592308546602726` gate; epoch-192 subset MSE was `0.004912700` at learning rate `6.86e-04`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:19:34Z`, the selected full CUDA run (PID 9970) reached epoch 193/300. Best full-validation MSE remains `0.004795069` from epoch 190; epoch-193 subset MSE was `0.004901727` at learning rate `6.86e-04`, and epoch-190 quantization diagnostics were finite with zero saturation. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:19:50Z`, the selected full CUDA run (PID 9970) reached epoch 194/300. Best full-validation MSE remains `0.004795069`; epoch-194 subset MSE was `0.004932461` at learning rate `6.86e-04`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:20:43Z`, the selected full CUDA run (PID 9970) reached epoch 196/300. Best full-validation MSE remains `0.004795069`; epoch-196 subset MSE was `0.004979096` at learning rate `6.86e-04`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:20:58Z`, the selected full CUDA run (PID 9970) reached epoch 197/300. Best full-validation MSE remains `0.004795069`; epoch-197 subset MSE was `0.005011532` at learning rate `6.86e-04`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:21:22Z`, the selected full CUDA run (PID 9970) reached epoch 198/300. Best full-validation MSE remains `0.004795069`; epoch-198 subset MSE was `0.004935798` at learning rate `6.86e-04`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:21:43Z`, the selected full CUDA run (PID 9970) reached epoch 199/300. Best full-validation MSE remains `0.004795069`; epoch-199 subset MSE was `0.004889924` at learning rate `6.86e-04`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:22:36Z`, the selected full CUDA run (PID 9970) reached epoch 201/300. Epoch 200 full-validation MSE was `0.004845256` (mean/median `11.8733%/8.6034%`), so best remains `0.004795069`; epoch-201 subset MSE was `0.004938708` at learning rate `4.80e-04`. Epoch-200 quantization diagnostics were finite with zero saturation. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:22:59Z`, the selected full CUDA run (PID 9970) reached epoch 202/300. Best full-validation MSE remains `0.004795069`; epoch-202 subset MSE was `0.004812689` at learning rate `4.80e-04`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:23:19Z`, the selected full CUDA run (PID 9970) reached epoch 203/300. Best full-validation MSE remains `0.004795069`; epoch-203 subset MSE was `0.004827526` at learning rate `4.80e-04`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:23:38Z`, the selected full CUDA run (PID 9970) reached epoch 204/300. Best full-validation MSE remains `0.004795069`; epoch-204 subset MSE was `0.004867367` at learning rate `4.80e-04`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:24:29Z`, the selected full CUDA run (PID 9970) reached epoch 206/300. Best full-validation MSE remains `0.004795069`; epoch-206 subset MSE was `0.004915196` at learning rate `4.80e-04`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:24:46Z`, the selected full CUDA run (PID 9970) reached epoch 207/300. Best full-validation MSE remains `0.004795069`; epoch-207 subset MSE was `0.004887189` at learning rate `4.80e-04`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:25:37Z`, the selected full CUDA run (PID 9970) reached epoch 209/300. Best full-validation MSE remains `0.004795069`; epoch-209 subset MSE was `0.004828274` at learning rate `4.80e-04`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:26:28Z`, the selected full CUDA run (PID 9970) reached epoch 211/300. Epoch 210 full-validation MSE was `0.004805480` (mean/median `11.7865%/8.4736%`), so best remains `0.004795069`; epoch-211 subset MSE was `0.004868277` at learning rate `4.80e-04`. Epoch-210 quantization diagnostics were finite with zero saturation. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:26:47Z`, the selected full CUDA run (PID 9970) reached epoch 212/300. Best full-validation MSE remains `0.004795069`; epoch-212 subset MSE was `0.004859009` at learning rate `3.36e-04`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:27:06Z`, the selected full CUDA run (PID 9970) reached epoch 213/300. Best full-validation MSE remains `0.004795069`; epoch-213 subset MSE was `0.004830834` at learning rate `3.36e-04`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:27:59Z`, the selected full CUDA run (PID 9970) reached epoch 215/300. Best full-validation MSE remains `0.004795069`; epoch-215 subset MSE was `0.004805398` at learning rate `3.36e-04`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:28:17Z`, the selected full CUDA run (PID 9970) reached epoch 216/300. Best full-validation MSE remains `0.004795069`; epoch-216 subset MSE was `0.004822767` at learning rate `3.36e-04`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:28:39Z`, the selected full CUDA run (PID 9970) reached epoch 217/300. Best full-validation MSE remains `0.004795069`; epoch-217 subset MSE was `0.004816709` at learning rate `3.36e-04`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:29:27Z`, the selected full CUDA run (PID 9970) reached epoch 219/300. Best full-validation MSE remains `0.004795069`; epoch-219 subset MSE was `0.004826772` at learning rate `3.36e-04`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:29:45Z`, the selected full CUDA run (PID 9970) reached epoch 220/300 and produced a new best full-validation MSE of `0.004757788` (mean/median `11.7592%/8.4964%`), below the `0.00592308546602726` gate. Epoch-220 quantization diagnostics were finite with zero saturation; the run remains active. Acceptance/parity have not started, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:30:36Z`, the selected full CUDA run (PID 9970) reached epoch 222/300. Best full-validation MSE remains `0.004757788` from epoch 220; epoch-222 subset MSE was `0.004807688` at learning rate `3.36e-04`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:30:55Z`, the selected full CUDA run (PID 9970) reached epoch 223/300. Best full-validation MSE remains `0.004757788`; epoch-223 subset MSE was `0.004851211` at learning rate `3.36e-04`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:31:49Z`, the selected full CUDA run (PID 9970) reached epoch 225/300. Best full-validation MSE remains `0.004757788`; epoch-225 subset MSE was `0.004813220` at learning rate `3.36e-04`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:32:08Z`, the selected full CUDA run (PID 9970) reached epoch 226/300. Best full-validation MSE remains `0.004757788`; epoch-226 subset MSE was `0.004857902` at learning rate `3.36e-04`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:32:29Z`, the selected full CUDA run (PID 9970) reached epoch 227/300. Best full-validation MSE remains `0.004757788`; epoch-227 subset MSE was `0.004798671` at learning rate `3.36e-04`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:32:49Z`, the selected full CUDA run (PID 9970) reached epoch 228/300. Best full-validation MSE remains `0.004757788`; epoch-228 subset MSE was `0.004788417` at learning rate `3.36e-04`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:33:08Z`, the selected full CUDA run (PID 9970) reached epoch 229/300. Best full-validation MSE remains `0.004757788`; epoch-229 subset MSE was `0.004831815` at learning rate `3.36e-04`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:34:00Z`, the selected full CUDA run (PID 9970) reached epoch 231/300 and produced a new best full-validation MSE of `0.004731534` (mean/median `11.6726%/8.4176%`), below the `0.00592308546602726` gate. Epoch-230 quantization diagnostics were finite with zero saturation; the run remains active. Acceptance/parity have not started, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:34:20Z`, the selected full CUDA run (PID 9970) reached epoch 232/300. Best full-validation MSE remains `0.004731534`; epoch-232 subset MSE was `0.004813364` at learning rate `3.36e-04`, with finite metrics. Epoch-230 quantization diagnostics were finite with zero saturation. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:34:41Z`, the selected full CUDA run (PID 9970) reached epoch 233/300. Best full-validation MSE remains `0.004731534`; epoch-233 subset MSE was `0.004804971` at learning rate `3.36e-04`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:35:35Z`, the selected full CUDA run (PID 9970) reached epoch 235/300. Best full-validation MSE remains `0.004731534`; epoch-235 subset MSE was `0.004790494` at learning rate `3.36e-04`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:35:54Z`, the selected full CUDA run (PID 9970) reached epoch 236/300. Best full-validation MSE remains `0.004731534`; epoch-236 subset MSE was `0.004768882` at learning rate `3.36e-04`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:36:15Z`, the selected full CUDA run (PID 9970) reached epoch 237/300. Best full-validation MSE remains `0.004731534`; epoch-237 subset MSE was `0.004866399` at learning rate `3.36e-04`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:36:38Z`, the selected full CUDA run (PID 9970) reached epoch 238/300. Best full-validation MSE remains `0.004731534`; epoch-238 subset MSE was `0.004795123` at learning rate `3.36e-04`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:36:56Z`, the selected full CUDA run (PID 9970) reached epoch 239/300. Best full-validation MSE remains `0.004731534`; epoch-239 subset MSE was `0.004788629` at learning rate `3.36e-04`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:37:52Z`, the selected full CUDA run (PID 9970) reached epoch 241/300. Epoch 240 full-validation MSE was `0.004790686` (mean/median `11.7620%/8.4635%`), so best remains `0.004731534`; epoch-241 subset MSE was `0.004828186` at learning rate `2.35e-04`. Epoch-240 quantization diagnostics were finite with zero saturation. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:38:21Z`, the selected full CUDA run (PID 9970) reached epoch 242/300. Best full-validation MSE remains `0.004731534`; epoch-242 subset MSE was `0.004770032` at learning rate `2.35e-04`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:38:42Z`, the selected full CUDA run (PID 9970) reached epoch 243/300. Best full-validation MSE remains `0.004731534`; epoch-243 subset MSE was `0.004747338` at learning rate `2.35e-04`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:39:07Z`, the selected full CUDA run (PID 9970) reached epoch 244/300. Best full-validation MSE remains `0.004731534`; epoch-244 subset MSE was `0.004772191` at learning rate `2.35e-04`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:39:29Z`, the selected full CUDA run (PID 9970) reached epoch 245/300. Best full-validation MSE remains `0.004731534`; epoch-245 subset MSE was `0.004766424` at learning rate `2.35e-04`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:39:51Z`, the selected full CUDA run (PID 9970) reached epoch 246/300. Best full-validation MSE remains `0.004731534`; epoch-246 subset MSE was `0.004793135` at learning rate `2.35e-04`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:40:15Z`, the selected full CUDA run (PID 9970) reached epoch 247/300. Best full-validation MSE remains `0.004731534`; epoch-247 subset MSE was `0.004771522` at learning rate `2.35e-04`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:40:41Z`, the selected full CUDA run (PID 9970) reached epoch 248/300. Best full-validation MSE remains `0.004731534`; epoch-248 subset MSE was `0.004786341` at learning rate `2.35e-04`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:41:03Z`, the selected full CUDA run (PID 9970) reached epoch 249/300. Best full-validation MSE remains `0.004731534`; epoch-249 subset MSE was `0.004760153` at learning rate `2.35e-04`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:41:21Z`, the selected full CUDA run (PID 9970) reached epoch 250/300. Full-validation MSE was `0.004733299` (mean/median `11.5913%/8.2702%`), so best remains `0.004731534`; epoch-250 quantization diagnostics were finite with zero saturation. The run remains active; acceptance/parity have not started, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:41:45Z`, the selected full CUDA run (PID 9970) reached epoch 251/300. Best full-validation MSE remains `0.004731534`; epoch-251 subset MSE was `0.004815784` at learning rate `2.35e-04`, with finite metrics. Epoch-250 quantization diagnostics were finite with zero saturation. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:42:11Z`, the selected full CUDA run (PID 9970) reached epoch 252/300. Best full-validation MSE remains `0.004731534`; epoch-252 subset MSE was `0.004825202` at learning rate `1.65e-04`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:42:35Z`, the selected full CUDA run (PID 9970) reached epoch 253/300. Best full-validation MSE remains `0.004731534`; epoch-253 subset MSE was `0.004782089` at learning rate `1.65e-04`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:43:01Z`, the selected full CUDA run (PID 9970) reached epoch 255/300. Best full-validation MSE remains `0.004731534`; epoch-255 subset MSE was `0.004751572` at learning rate `1.65e-04`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:43:26Z`, the selected full CUDA run (PID 9970) reached epoch 256/300. Best full-validation MSE remains `0.004731534`; epoch-256 subset MSE was `0.004815840` at learning rate `1.65e-04`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:43:49Z`, the selected full CUDA run (PID 9970) reached epoch 257/300. Best full-validation MSE remains `0.004731534`; epoch-257 subset MSE was `0.004789773` at learning rate `1.65e-04`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:44:16Z`, the selected full CUDA run (PID 9970) reached epoch 258/300. Best full-validation MSE remains `0.004731534`; epoch-258 subset MSE was `0.004748223` at learning rate `1.65e-04`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:44:37Z`, the selected full CUDA run (PID 9970) reached epoch 259/300. Best full-validation MSE remains `0.004731534`; epoch-259 subset MSE was `0.004727217` at learning rate `1.65e-04`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:45:03Z`, the selected full CUDA run (PID 9970) reached epoch 260/300 and produced a new best full-validation MSE of `0.004697189` (mean/median `11.5528%/8.2637%`), below the `0.00592308546602726` gate. Epoch-260 quantization diagnostics were finite with zero saturation; the run remains active. Acceptance/parity have not started, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:45:34Z`, the selected full CUDA run (PID 9970) reached epoch 261/300. Best full-validation MSE remains `0.004697189`; epoch-261 subset MSE was `0.004726398` at learning rate `1.65e-04`, with finite metrics. Epoch-260 quantization diagnostics were finite with zero saturation. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:45:59Z`, the selected full CUDA run (PID 9970) reached epoch 262/300. Best full-validation MSE remains `0.004697189`; epoch-262 subset MSE was `0.004760609` at learning rate `1.65e-04`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:46:23Z`, the selected full CUDA run (PID 9970) reached epoch 263/300. Best full-validation MSE remains `0.004697189`; epoch-263 subset MSE was `0.004727599` at learning rate `1.65e-04`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:46:44Z`, the selected full CUDA run (PID 9970) reached epoch 264/300. Best full-validation MSE remains `0.004697189`; epoch-264 subset MSE was `0.004775600` at learning rate `1.65e-04`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:47:07Z`, the selected full CUDA run (PID 9970) reached epoch 265/300. Best full-validation MSE remains `0.004697189` from epoch 260; epoch-265 subset MSE was `0.004758749` at learning rate `1.65e-04`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:48:46Z`, the selected full CUDA run (PID 9970) reached epoch 269/300. Best full-validation MSE remains `0.004697189` from epoch 260; epoch-269 subset MSE was `0.004736535` at learning rate `1.65e-04`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:49:30Z`, the selected full CUDA run (PID 9970) reached epoch 271/300. Epoch-270 full-validation MSE was `0.004725720` and best remains `0.004697189` from epoch 260; epoch-271 subset MSE was `0.004792765` at learning rate `1.65e-04`, with finite metrics and zero saturation in the epoch-270 quantization diagnostics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:50:12Z`, the selected full CUDA run (PID 9970) reached epoch 273/300. Best full-validation MSE remains `0.004697189` from epoch 260; epoch-273 subset MSE was `0.004803149` at learning rate `1.65e-04`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:51:19Z`, the selected full CUDA run (PID 9970) reached epoch 276/300. Best full-validation MSE remains `0.004697189` from epoch 260; epoch-276 subset MSE was `0.004753762` at learning rate `1.65e-04`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:51:54Z`, the selected full CUDA run (PID 9970) reached epoch 277/300. Best full-validation MSE remains `0.004697189` from epoch 260; epoch-277 subset MSE was `0.004783127` at learning rate `1.65e-04`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:52:25Z`, the selected full CUDA run (PID 9970) reached epoch 279/300. Best full-validation MSE remains `0.004697189` from epoch 260; epoch-279 subset MSE was `0.004744694` at learning rate `1.15e-04`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:52:58Z`, the selected full CUDA run (PID 9970) reached epoch 280/300 and produced a new best full-validation MSE of `0.004661038` (mean/median `11.4690%/8.1278%`), below the `0.00592308546602726` gate. Epoch-280 quantization diagnostics were finite with zero saturation; the run remains active. Acceptance/parity have not started, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:53:32Z`, the selected full CUDA run (PID 9970) reached epoch 281/300. Best full-validation MSE remains `0.004661038` from epoch 280; epoch-281 subset MSE was `0.004710191` at learning rate `1.15e-04`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:54:10Z`, the selected full CUDA run (PID 9970) reached epoch 282/300. Best full-validation MSE remains `0.004661038` from epoch 280; epoch-282 subset MSE was `0.004717895` at learning rate `1.15e-04`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:54:45Z`, the selected full CUDA run (PID 9970) reached epoch 284/300. Best full-validation MSE remains `0.004661038` from epoch 280; epoch-284 subset MSE was `0.004709397` at learning rate `1.15e-04`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:55:30Z`, the selected full CUDA run (PID 9970) reached epoch 286/300. Best full-validation MSE remains `0.004661038` from epoch 280; epoch-286 subset MSE was `0.004756575` at learning rate `1.15e-04`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:56:09Z`, the selected full CUDA run (PID 9970) reached epoch 288/300. Best full-validation MSE remains `0.004661038` from epoch 280; epoch-288 subset MSE was `0.004758514` at learning rate `1.15e-04`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:56:49Z`, the selected full CUDA run (PID 9970) reached epoch 289/300. Best full-validation MSE remains `0.004661038` from epoch 280; epoch-289 subset MSE was `0.004694135` at learning rate `1.15e-04`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:57:27Z`, the selected full CUDA run (PID 9970) reached epoch 291/300. Epoch-290 full-validation MSE was `0.004712017`; best remains `0.004661038` from epoch 280. Epoch-291 subset MSE was `0.004734016` at learning rate `8.07e-05`, with finite metrics and zero saturation in epoch-290 quantization diagnostics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:58:25Z`, the selected full CUDA run (PID 9970) reached epoch 292/300. Best full-validation MSE remains `0.004661038` from epoch 280; epoch-292 subset MSE was `0.004731960` at learning rate `8.07e-05`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:59:12Z`, the selected full CUDA run (PID 9970) reached epoch 295/300. Best full-validation MSE remains `0.004661038` from epoch 280; epoch-295 subset MSE was `0.004732853` at learning rate `8.07e-05`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T14:59:47Z`, the selected full CUDA run (PID 9970) reached epoch 297/300. Best full-validation MSE remains `0.004661038` from epoch 280; epoch-297 subset MSE was `0.004787953` at learning rate `8.07e-05`, with finite metrics. Acceptance/parity have not started; the run remains active, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T15:00:32Z`, the selected full CUDA run (PID 9970) reached epoch 299/300. Best full-validation MSE remains `0.004661038` from epoch 280; epoch-299 subset MSE was `0.004696114` at learning rate `8.07e-05`, with finite metrics. Acceptance/parity have not started; the run remains active for the final epoch, and the required 16 new videos are still outstanding (3 pre-existing MP4s present).
- At `2026-07-22T15:00:46Z`, the selected full CUDA run completed epoch 300/300 and passed final acceptance: best full-validation MSE `0.004657660982012749` (gate `0.00592308546602726`), finite metrics/diagnostics/checkpoint state, and zero saturation. The accepted checkpoint SHA-256 is `c6d7b737123615e3ca9dfe249bee18be75dfceac6f9ada3b13e2efb3010dad09`. Rollout parity also passed on 256 fixed validation samples with maximum absolute error `0.0` (atol `1e-06`), finite outputs, and zero saturation. Checklist gates 7 and 8 are now complete; exactly 16 new robot videos and final handoff validation remain outstanding (3 pre-existing MP4s present).
- At `2026-07-22T15:10:40Z`, the accepted all-robot recording wrapper is active after a PPO Orbax backward-compatibility fix that excludes the five new bootstrap quantization config keys from old checkpoint-schema comparison; `ppo.py` passes `py_compile`. Robots 0–2 have produced three new MP4s (16 remain required total, with 3 pre-existing MP4s), and robot 3 is currently recording. Final video validation, contact sheets, manifest, and checklist gates 9–10 remain pending.
- At `2026-07-22T15:11:47Z`, the recording wrapper completed robots 0–3 (`unitree_a1`, `unitree_go1`, `unitree_go2`, `anymal_b`) and advanced to robot 4 (`anymal_c`). Four authoritative mapping rows are present; the wrapper remains active. Final validation/contact sheets/manifest and checklist gates 9–10 remain pending.
- At `2026-07-22T15:14:18Z`, the recording wrapper has completed robots 0–5 and written six mapping rows; robot 6 (`barkour_vb`) is actively recording. Seven dated MP4s are visible (the seventh is the in-progress output); final video validation/contact sheets/manifest and checklist gates 9–10 remain pending.
- At `2026-07-22T15:15:22Z`, robots 0–6 have completed successfully and seven mapping rows are present; robot 7 (`badger`) is actively recording. Eight dated MP4s are visible including the in-progress output. Final video validation/contact sheets/manifest and checklist gates 9–10 remain pending.
- At `2026-07-22T15:16:15Z`, robot 7 (`badger`) completed and the wrapper advanced to robot 8 (`bittle`); eight mapping rows are present. Nine dated MP4s are visible including the in-progress output; final validation/contact sheets/manifest and checklist gates 9–10 remain pending.
- At `2026-07-22T15:18:27Z`, robot 8 (`bittle`) completed and the wrapper advanced to robot 9 (`unitree_h1`); nine mapping rows are present. Ten dated MP4s are visible including the in-progress output. Final validation/contact sheets/manifest and checklist gates 9–10 remain pending.
- At `2026-07-22T15:19:48Z`, robot 9 (`unitree_h1`) completed and the wrapper advanced to robot 10 (`unitree_g1`); ten mapping rows are present. Eleven dated MP4s are visible including the in-progress output. Final validation/contact sheets/manifest and checklist gates 9–10 remain pending.
- At `2026-07-22T15:21:24Z`, robot 10 (`unitree_g1`) completed and the wrapper advanced to robot 11 (`talos`); eleven mapping rows are present. Twelve dated MP4s are visible including the in-progress output. Final validation/contact sheets/manifest and checklist gates 9–10 remain pending.
- At `2026-07-22T15:22:54Z`, robot 11 (`talos`) completed and the wrapper advanced to robot 12 (`robotis_op3`); twelve mapping rows are present. Thirteen dated MP4s are visible including the in-progress output. Final validation/contact sheets/manifest and checklist gates 9–10 remain pending.
- At `2026-07-22T15:26:10Z`, robot 12 (`robotis_op3`) completed (its MP4 finalized after a longer render), and the wrapper advanced to robot 13 (`nao_v5`); twelve mapping rows are present. Fourteen dated MP4s are visible including the in-progress output. Final validation/contact sheets/manifest and checklist gates 9–10 remain pending.
- At `2026-07-22T15:27:37Z`, robot 14 (`cassie`) completed and the wrapper advanced to the final robot 15 (`hexapod`); fourteen mapping rows are present. Fifteen dated MP4s are visible including the in-progress output. Final validation/contact sheets/manifest and checklist gates 9–10 remain pending.


### 2026-07-22 — Gates 9–10 complete

- All 16 configured record_robots entries produced exactly one newly generated MP4 directly under experiments/videos; the three pre-existing MP4s remain preserved and excluded via recording/before_all_videos.txt.
- Authoritative mapping: snn/experiments/snn_v2_quantrick/20260722T053851Z/recording/video_mapping.tsv; validation: recording/video_validation.json (validated true, 16/16, OpenCV FFmpeg enabled, 10.0-second/500-frame metadata, no errors); contact sheets: recording/contact_sheets/ (16 files).
- All 16 recording logs have empty forbidden-match lists (no traceback, NaN/Inf action, checkpoint mismatch, or teacher fallback). Final handoff manifest is snn/experiments/snn_v2_quantrick/20260722T053851Z/run_manifest.json, including checkpoint SHA-256, 24/8 mixed/all quantization, accepted MSE/parity metrics, and robot/video evidence.
